
{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "openclow-vllm-openai.serviceAccountName" . }}
  labels:
    {{- include "openclow-vllm-openai.labels" . | nindent 4 }}
{{- end -}}
